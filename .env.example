# Local dashboard development only. Never put real production secrets in Git.
MONITOR_ENDPOINT=https://monitor-api.example.com/v1/snapshot
MONITOR_TOKEN=replace-with-the-token-created-by-install-collector.sh
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-long-random-password
SESSION_SECRET=replace-with-at-least-32-random-bytes
